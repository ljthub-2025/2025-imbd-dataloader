numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
xgboost>=2.0
optuna>=3.4
joblib>=1.3
pyyaml>=6.0
matplotlib>=3.7
seaborn>=0.12

# PyTorch (CUDA 12.4)
# 請依照官方指引安裝對應 CUDA 版本的 torch，例如：
# pip install --index-url https://download.pytorch.org/whl/cu124 torch torchvision torchaudio
# 本檔未固定 torch 版本，避免與你的 CUDA/驅動版本衝突

